passwordless ssh

login to the machine u want to ssh from and do ssh-keygen
goto cd .ssh/
cat id_rsa.pub
copy it
now ssh to the machine u want to ssh to
go to .ssh/
paste the text we copied from id_rsa.pub to .ssh/authorised_keys
voila now we can do passwordless ssh from system 1 to system 2
